---
course: AIDD Bioinformatics
chapter_no: 7
chapter_title: NGS data Analysis on Bash (Gene Expression Using Command Line)
lesson_no: 10.0
lesson_title: SAM and Bam File Indexing and Sorting
source_subtitle: G:/医药数据处理与可视化/AIDD Bioinformatics/Bioinformatics Data Analysis Crash Course Python R and Linux/7. NGS data Analysis on Bash (Gene Expression Using Command Line)/10. SAM and Bam File Indexing and Sorting.srt
duration_minutes: 7.5
language: zh-CN
cleaning: WEBVTT timecodes removed; professional terminology corrections applied; sentences reflowed.
---

# SAM and Bam File Indexing and Sorting

在上一个视频中,我们结束了索引部分,但我忘了讨论
关于高顶礼帽。
顶帽也是我们对齐部分所需的工具。
它是 RNA 扫视的连接映射器,类似于 DNA 扫视。
我们还需要 RNA 扫视。
它通常使用顶帽对齐哺乳动物大小的基因组使用眼睛吞吐量短
阅读对齐器领结。
因此,它是建立在领结上的,但它在 RNA 扫视上起作用。
分析映射结果来识别轴突之间的剪接点。
我希望你能理解这一点。
所以在上一讲中,我们创建了一个 SAM 文件,但基本上 SAM 就是。
SAM 文件是一种序列比对图格式,是存储
大的核苷酸序列比对。
SAM 的目标是成为一种足够灵活的格式,以存储生成的所有对齐信息
通过各种对齐程序,并且也足够简单,可以通过对齐程序轻松生成
并将其转换为现有的对齐格式,且文件大小紧凑。
它占用较小的尺寸。
它允许大部分对齐操作在流中工作,而无需加载整个
对齐到内存中。
例如,它允许按基因组位置对文件进行索引,以便有效地检索
将所有对齐过程集中到一个轨迹上。
它还旨在保持该文件的大小。
它还提供各种用于操作对齐的实用程序。
所以这些事情都发生在 SAM 文件中。
因此,samtools是提供各种用于操纵比对的工具
转换成其他不同类型的对齐格式。
例如BAM格式。
BAM 是 SAM 的人类可读格式。
人类可以读懂这种格式。
但是BAM是二元比对图。
所以 BAM 不包含这类信息。
例如,如果您创建 BAM 文件,则无法将其可视化。
你看不到它。
还有编程语言和类似的东西。
BAM文件就是这样的。
因此,为了转换和排序 SAM 文件,我们需要 samtools。
首先我们要安装 samtools,然后我们要转换 SAM 文件
转换为 BAM 文件,然后我们对 BAM 和 SAM 文件进行排序。
我们就这么做吧。
因此将此命令复制到我们确实执行的文本文件中。
所以这里我们要安装 samtools,在我的情况下已经完成了。
我只是想清理一下并看看我是否在正确的文件夹中。
我是。
所以我只是要把它放在那里。
这里存在一个 SAM 文件到 BAM 文件的转换模式。
因此,请复制此命令并将其粘贴到我们的终端中。
所以 samtools视图,是的,对齐点 SAM 转变为对齐点 BAM。
事实也是如此。
应该有一个文件。
是的,对齐点 BAM 文件在这里。
对齐点 SAM 文件在这里。
所以我们要对SAM和BAM文件一起排序。
排序、对点 BAM 进行排序并对齐点 BAM。
所以我认为应该有一个错误,我看到要创建的排序点 BAM 文件
来自对齐点 BAM 文件。
因此对点 BAM 进行排序。
我觉得这个应该是有问题的。
是的,有问题。
现在应该有一个文件。
排序,获取,排序点 BAM。
是的。
就是这样。
因此获取、排序点BAM文件。
现在您可以明白为什么我称它为非人类可读的。
所以它是一个机器可读的文件。
这是一个二进制比对文件。
这就是我们使用的文件。
例如,如果我们拿到了 SAM 文件,我们就不能用 SAM 文件来操作该文件
分解成我们在进一步过程中需要的不同的其他步骤。
所以我们需要一个二进制文件。
我们需要一个二进制比对文件,以便机器能够从中学习,
可以用。
因此我们创建了该文件,并将 SAM 文件更改为 BAM 文件。
然后我们使用 samtools对该 BAM 文件进行排序。
但如果你想使用 BAM 文件,那里也有实用程序,
即所谓的 BAM 工具。
您可以使用 BAM 工具。
如果我能看到我已经安装了 BAM 工具,那么我应该已经安装了。
我也在这里获得了 BAM 工具。
我可以使用该 BAM 工具在 BAM 和其他格式之间进行转换。
我可以使用帳戶。
我可以使用标题索引和许多其他可以与 SAM 文件一起使用的东西。
我也可以与 BAM 文件一起使用。
这就是本次讲座的全部内容。
我认为我们已经成功地结束了本次讲座。
请记住,我们正在使用仅包含两个读取的演示文件。
所以 NGIS 数据排序没有这么快。
它更快了。
我只需输入命令,然后命令就会完成。
我们现在只需要一个参考注释文件,然后我们将剪切注释
来自该注释基因组文件。
这就是本讲的内容,在下一讲中,我们将下载一个 gdf ​​文件
我们的基因组。
我还想向您展示如何下载 gdf ​​文件。
然后我们要提取 RNA 表达,这也是 DG 的差异
表达的基因。
我们还将分析他们的基因并绘制热图。
所以我认为你在上次会议中已经做过了,这不是必需的
在本节中也是如此。
因此,下一堂课应该是我们这个命令的最后一节课
線段。
这就是本次讲座的全部内容。
