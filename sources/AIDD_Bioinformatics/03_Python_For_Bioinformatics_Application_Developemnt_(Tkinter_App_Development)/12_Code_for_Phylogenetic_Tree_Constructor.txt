---
course: AIDD Bioinformatics
chapter_no: 3
chapter_title: Python For Bioinformatics Application Developemnt (Tkinter App Development)
lesson_no: 12.0
lesson_title: Code for Phylogenetic Tree Constructor
source_subtitle: G:/医药数据处理与可视化/AIDD Bioinformatics/Bioinformatics Data Analysis Crash Course Python R and Linux/3. Python For Bioinformatics Application Developemnt (Tkinter App Development)/12. Code for Phylogenetic Tree Constructor.srt
duration_minutes: 9.9
language: zh-CN
cleaning: WEBVTT timecodes removed; professional terminology corrections applied; sentences reflowed.
---

# Code for Phylogenetic Tree Constructor

所以在屏幕上您可以看到我已经准备了一些代码以及所需的重要模块。
安装并运行我们的系统发育树代码。
首先,有一些库正在本课程中使用。
第一个是 Tinker 库,还有 Kinter。
它用于创建 GUI 窗口。
因此,kinder 也包含在 Python 中。
所以你不需要安装任何额外的东西,但是如果你没有这个模块,你可以使用此命令 pip install tk 来安装它。
我也将在课程资源中提供此内容。
因此,您就有了用于生物信息学操作(例如阅读序列比对和系统发育树的构建)的 bio 模块。
安装时,您也可以使用 pip 安装 bio Python 包。
因此,它是 pip install bio Python,然后我们有用于绘制和保存系统发育树图像的 metro 库。
所以这个库通常与图像一起使用。
您可以使用 pip 命令安装它。
首先让我向你展示如何做到这一点。
首先,你在这里进行如下设置,并且有一个正在运行的 Python 终端。
在获得运行终端后,您只需粘贴命令 pip install tk,它就会安装该库,因为我已经安装了它。
所以无需再次安装。
bio Python 和 metro 和 met lot lip 也是如此。
这就是安装运行此工具所需的所有库和代码的方法。
别担心,我也会给你这个文件。
我还会给你运行该工具所需的代码。
现在让我们开始使用工具。
首先,我们需要导入构建系统发育树所需的模块和库。
首先,像往常一样,我们将导入我们的 Kindle 图书馆及其现场日志以显示一些消息。
然后我们将转到
从 bio 导入对齐数据。
那么对齐数据是做什么的呢?
对齐数据是一个库,作为对齐对象输入和输出。
因此,bio align 接口刻意与 bio sec I/O 非常相似,实际上两者内部相连。
因此两个模块使用同一套文件格式。
从用户角度来看,您可以读入包含一个或多个比对的文件,并且可以使用 bio sec 读取其中的序列。
所以让我为您简化一下。
从 bio Python 模块中,您必须导入另一个模块,从 bio Python 库中,您必须导入另一个模块,该模块称为 aligned data align。
因此,align I/O 用于对齐您为构建系统发育树提供的序列。
因此,之后您需要另一个库,即 bio.phylo.tree 构建。
您必须导入它的一些算法,必须导入距离计算器,然后还必须导入基于距离的树构造函数。
因此我们正在构建这棵树。
基于距离的方法,我们之前在第一讲中讨论过。
之后,我们将导入用于绘制系统发育树的绘制模块。
然后我们必须导入我们的 matplotlib,然后我们必须。
然后创建一个直接的界面,这样它就为我们提供了绘图的方式。
它还可以在屏幕上打开图形并充当 GUI 管理器。
然后我们有了这个库,我们将导入它。
所以基本上这个代码中有三个库在运行。
首先,我们将创建一个函数来生成我们的系统发育树。
因此,我们将创建一个名为生成系统发育树的函数。
然后,我们将要求用户选择对齐序列文件。
因此,我们将为其提供一个包含多个序列的序列文件,并将其对齐。
因此,我们将创建一个输入文件路径。
之后,我们会询问文件名。
因此,我们将选择序列文件,这是一个更快的文件。
您可以说 dot FA 文件,然后
另一件事是你想包含在其中。
你也可以添加自己的文件格式。
但对我来说,更快的文件效果很好。
所以我继续这样做。
因此,如果没有选择文件,那么我们应该说返回,它将返回到自己的位置,并且不会选择任何内容。
然后我们会要求用户选择
在哪里保存它们的系统发育树图像。
首先,它会为我们创建一个系统发育树,当构建系统发育树时,它会询问我们在哪里保存它的系统发育树文件。
然后我们有一些额外的函数,我们用它来开发算法。
所以首先我们必须读取对齐文件,为此我们将使用它
对齐 IO 模块,然后读取对齐 IO 模块后,我们将
输入文件路径。
通常在您提供序列文件时完成此操作,然后我们将计算它们的距离度量。
因此,计算距离度量。
我们将为其提供一个距离度量变量,然后使用计算器变量来获取距离的方法。
因此它将返回一个比对或多个序列比对对象的距离度量。
然后我们将使用邻接连接法构造树。
因此,在使用构造函数变量后,我们将使用构造函数变量,我们将初始化
距离树构造器。
因此,树变量将构造邻居连接距离度量树。
然后我们必须绘制树。
因此,图形将是 12 x 8,您也可以更改它。
是的,我认为我们应该将其改为
20 x 10。
所以我认为效果会很好。
然后我们必须画树。
之后我们将绘制它的轴点。
然后,我们将图形保存到我们最近在此处生成的输出文件路径,然后关闭此库。
之后,您必须显示文件遗传树生成成功的成功消息。
这是针对生物信息学文件遗传树构造器的基本算法开发。
然后你必须为你自己的工具创建一些窗口。
所以首先,你必须创建一个 GI 窗口,它是 Kinter 库的变量根。
之后你必须给它一个根标题。
所以它被称为文件遗传树生成器,然后你就得到了 800 x 600 的根几何形状。
然后你必须为其创建级别。
假设我们点击下面选择一个行序列并生成一个文件遗传树。
我认为我应该运行这段代码,然后向大家展示它是如何工作的。
所以我认为它应该可以正常运行。
此处我们可以看到,我们有 Kinter 模块,可以运行文件遗传树构造器。
此处您可以看到,单击下方以选择对齐序列文件并生成此处写明的文件遗传树。
这就是您可以创建自己的标签的方法。
之后,您必须为按钮创建一个函数。
因此,我们将为按钮初始化变量,然后我们必须选择文件,然后我们将使用命令生成文件遗传树,然后我们将为其提供一些包装,就像我们将要使用的参数 10 一样。
然后我们将为我们听到的文件遗传树生成成功成功消息创建一个标签。
显示成功消息。
所以它是成功消息的标签。
之后你必须运行库间事件循环。
所以这是一个 60 行代码,作为文件遗传树生成。
所以基本上这就是你如何为文件遗传树构造器 GUI 应用程序编写代码,在下一讲中,我将向你展示文件遗传树构造器所需的文件。
然后。
我们将对齐一些序列并将其输入给我们的程序。
之后我们将创建它的 EHC 文件以使其与我们一起运行。
这就是您可以从比对序列文件创建自己的文件遗传树构造器的方法。
